# 番茄畅听净化模块 v1.9.20 (versionCode 192000)

> 目标应用：番茄畅听 `com.xs.fm`（实测 **`6.7.1.32`** / versionCode `671`）
> 框架要求：Root + Vector 或 LSPosed（xposedminversion 82，Android 8.0+）
> 实测设备：Realme RMX2202 / Android 14

## 版本支持

| 番茄畅听 | versionCode | 模块版本 | 状态 |
|---|---|---|---|
| **`6.7.1.32`** | `671` | **v1.9.20** | ✅ **当前版本，实测通过** |
| `6.7.1.16` | `671` | v1.9.10 | ✅ 实测通过（上一版，不再维护） |
| `6.6.7.32` | `667` | v1.9.4 / v1.9.5 | ✅ 实测通过（历史版本） |
| `6.6.4.32` | `664` | v1.9.2 | ✅ 实测通过（历史版本） |

⚠️ `6.7.1.16` → `6.7.1.32` 的 `versionCode` **都是 `671`**，混淆「名字表」没变，但**资源 id 的数值整体平移**，而且「我的」页连对象名都换了（`bpz`→`br7`、`e33`→`e4q`、`ccv`→`ce3`/`ce4`）。
**结论：versionCode 没变 ≠ 不用适配。**

## 本版更新

### 新增（「我的」页四类入口，从源头屏蔽）
- **我的消息 / 游戏中心**：快捷入口栏 `ce3` 里的 `u2` 入口卡。这些入口全在页头 AppBar 内，`hideChain()` 的「顶栏整链放弃」保护会让文本规则变成空操作（日志：`跳过隐藏(位于顶栏内): '游戏中心'`）；新增 `hideMineHeaderEntry()` 绕过该保护，但只藏「可点击入口卡」（带 25% 屏高护栏），**绝不越过 AppBar**
- **我的资产**：整个板块 `e4q` 隐藏（含 提现 / 金币余额(币) / 现金余额(元) / 剩余时长(分)）。走精确资源 id，不再用文本兜底 —— 文本兜底会一路爬到页头包装容器 `e2n`，把头像 / 昵称 / 个人主页一起吞掉
- **活动横幅（中秋好礼）**：`ej2` 内的「中秋好礼限时购 · 一件立减15%>」是**画在图片里的字**，节点上没有 text 也没有 content-desc，文本规则永远抓不到，只能按容器 id 屏蔽。同时把整张头部卡片 `ce4` 一起 GONE —— 只藏子项（`ce3`+`ej2`）会留下一块 1008×156 的空心圆角底，正是要避免的「大片空白」
- **VIP 促销卡**：`br7`（含「¥1开通」「立减」）。旧版 id `bpz` 在 6.7.1.32 已不存在，本版接上

### 新增（其它位置，v1.9.11 ~ v1.9.19 累积）
- **阅读页正文流「一站式购物卡」**：OneStop（Lynx）自绘、无 resource-id、不走广告总闸。短路其展示 / 请求策略后，广告在**请求之前**就被否掉，阅读器不再留占位（**连空白一起消失**）
- **章节末「看小视频免30分钟广告」入口行**：让生成它的工厂方法 `f22.q.a()/b()/c()` 返回 `null`，App 自行跳过添加该行，不报错也不留空白
- **首页整屏 VIP 促销浮层**：短路 JSBridge 方法 `showVipPromotionPopup()`，并对促销 Dialog `show()` 兜底
- **广告位总闸**：`AdConfigManager.checkAdAvailable` + `AdLynxHelper.checkIfRitAvailable` 双闸门

### 构建
- 新增 `module/build_local.sh`：**宿主机本地构建，不需要手机 root**
  - 编译 → d8 出 `classes.dex` → 替换进上一版已签名 APK → 就地补丁清单里的 `versionCode`/`versionName` → 用同一 keystore 重签 **v1+v2**
  - 产物可直接 `adb install -r` 覆盖安装

### 修复
- `XC_MethodHook.MethodHookParam` 补上 `method` 字段（Xposed stub 缺失，导致诊断代码无法编译）
- 新增的「我的」页入口隐藏做了 `ce4` 存在性判定（该 id 只在「我的」页出现），避免影响首页 / 阅读页

### 已知限制
- 「我的」页活动横幅 `ej2` 是**轮换位**：按容器 id 屏蔽 ⇒ 后续换成别的活动（春节、双十一…）也会一起消失；若只想屏蔽某一档活动，需要改成按内容 / 跳转目标过滤
- 正文流 OneStop 广告卡被短路后阅读器不再为它预留位置（有意为之，避免留白）；若官方将来把有用内容放进同一容器，也会一起不显示

## 安装

1. 手机已 Root，并装好 Vector 或 LSPosed
2. 安装本模块 APK
3. 在管理器里**启用本模块**，并在**作用域**中勾选「番茄畅听」
4. 强制停止番茄畅听（或重启手机）后重新打开

> 去广告与界面净化**不依赖登录**；想要完整 VIP 免听体验再登录即可（VIP 权益由服务端校验）。

## 产物

| 文件 | 说明 |
|---|---|
| `fanqie-enhance-v1.9.20.apk` | 本版模块（versionCode `192000` / versionName `1.9.20`） |
| `module/build_local.sh` | 宿主机本地构建脚本（**不需要 root，推荐**） |
| `module/build.sh` | 手机端 Termux 工具链构建脚本（需 root） |
